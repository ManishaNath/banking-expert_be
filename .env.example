# HTTP port for the Express API. Defaults to 8000 when not set.
PORT=8000

# Connection string for your MongoDB database.
# Example for MongoDB Atlas: mongodb+srv://<username>:<password>@<cluster-url>/<db-name>?retryWrites=true&w=majority
MONGO_URI=mongodb://127.0.0.1:27017/banking-expert

# Secret used for signing JWT access tokens. Use a long random string in production.
JWT_SECRET=replace-with-a-secure-random-string

# Moralis Web3 API key for fetching on-chain data.
MORALIS_API_KEY=replace-with-your-moralis-api-key

# Optional:  list of origins allowed to access the API via CORS.
# Example: http://localhost:3000,https://frontend.netlify.app
CORS_ALLOWED_ORIGINS=
